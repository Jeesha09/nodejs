// Modules - Encapsulaed Code (only share minimum)
// CommonJS, every file is module (by default)

const sayHi = (name) => {
    console.log(`Hello there ${name}`)
}

// export default
module.exports = sayHi